# local-only noise — journal.json, candidates.json, candidates-history.jsonl
# and scans.log ARE tracked (they are the eval's data)
*.bak
*.tmp
node_modules/
# scratch file the alert step writes; the notification body, not data
alert-body.md
